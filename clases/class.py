# Clases en python 
# Clases se inicializan en mayuscula y los nombres compuestos camelCase

# Atributos de la clase 

# Cosntructor : es el que crea la instancia (objeto) de la clase llamandose a si misma
    # Se define una funcion dentro de la clase def __init__(self, atributos)
    # Atributos self.atributo1 , self.atributo2
    # self palabra reservada que permite que la funcion se llame a si misma 
    
# Metodos son las acciones que puede ejecutar una clase (como son funciones se iniciliza con la palabra reservada def)
    # los metodos se llaman nombreObjeto.nombreMetodo()
    # los objetos de las clases heredan las caracteristicas de la clase
    # Se le debe poner a los metodos de clase self (le indica que el metodo depende de esa clase)
    
# HERENCIA DE CLASES una clase hereda otra clase
    #def NombreClase(ClaseHeredada)


